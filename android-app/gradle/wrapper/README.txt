This project expects the Gradle wrapper JAR at this path when building locally or in CI.
If it is missing, generate it once with a local Gradle installation using:

gradle wrapper

Then commit gradle/wrapper/gradle-wrapper.jar
